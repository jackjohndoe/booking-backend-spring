# Nixpacks configuration for Railway
# Railway will use this for build configuration

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps || npm install --legacy-peer-deps"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "npm start"

